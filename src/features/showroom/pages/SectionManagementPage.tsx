import { SectionManagementContainer } from '../containers';

/**
 * 섹션 관리 페이지
 */
export const SectionManagementPage = (): JSX.Element => {
  return <SectionManagementContainer />;
};

export default SectionManagementPage;
