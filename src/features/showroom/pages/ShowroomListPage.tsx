import { ShowroomListContainer } from '../containers';

/**
 * 쇼룸 리스트 페이지
 */
export const ShowroomListPage = (): JSX.Element => {
  return <ShowroomListContainer />;
};

export default ShowroomListPage;
